# Usar los MISMOS valores que pusiste en el .env de la raíz (para docker compose up -d db)
DATABASE_URL="postgresql://gymbo:cambia_esta_clave@localhost:5432/gymbogest"
JWT_SECRET="genera_una_clave_larga_y_aleatoria_aqui"
PORT=3001
